mutation register($registration: RegisterSubscriber!){
    register(registration: $registration) {
        msg
        error
        field
        password
        subscriber {
            _id
            username
        }
    }
}