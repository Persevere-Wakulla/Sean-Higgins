query getSubscriberTickerData($ticker: WatchTicker!) {
	getSubscriberTickerData(ticker: $ticker) {
        purchasePrice
        shares
        symbol
        subscriber {
            _id
        }
	}
}