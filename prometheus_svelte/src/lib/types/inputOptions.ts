export type InputOptions = {
    placeholder: string,
    type: string,
    name: string,
    id?: string,
    value?: FormDataEntryValue | string | number | null,
    autoComplete?: string,
    // onChangeCB?: Event<HTMLInputElement>
}